name: workflowCICD
on: push
jobs:
  test:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v2
      - name: Build the stack
        run:  git clone https://github.com/amalraj7171/workflowCICD.git && cd WorkflowCICD && docker-compose up -d
